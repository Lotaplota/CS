#include <stdio.h>

char *main(void)
{
    int i;
    float a, b, c;

    printf("insira os valores de i, A, B e C no formato #,#,#,#");

    switch (i)
    {
    case 1:
        printf("A:%f B: C:");
        break;
    
    default:
        break;
    }
}