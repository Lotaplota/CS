- **Surrogate keys** don't have anything to do with the real world, while **natural keys** have
- Composite keys are required when a column doesn't uniquely identifies its rows
- `DECIMAL(10,4)` is a decimal with 10 digits and 4 of those are after the comma

RDBMS
: Relational Database Management System

DQL
: Data Query Language (SELECT)

DDL
: Data Definition Language (CREATE, ALTER, DROP)

DCL
: Data Control Language

DML
: Data Manipulation Language (INSERT, UPDATE, DELETE)

SQL
: Structured Query Language

CRUD
: Create, read, update and delete